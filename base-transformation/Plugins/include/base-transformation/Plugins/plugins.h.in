#pragma once

/* 
 * Build-time plugin selection. 
 * These evaluate to 1 or 0 and do not define global macros to avoid clashes.
 */

#if @HAS_EIGEN@
#include <base-transformation/Plugins/eigen.h>
#endif

#if @HAS_PCL@
#include <base-transformation/Plugins/pcl.h>
#endif
